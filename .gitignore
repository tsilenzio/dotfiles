# Machine-specific state (logs, snapshots, bundle selection)
.state/
.bundles

# Loaded bundles symlink (points to .state/loaded/)
loaded

# Local testing cache (installers, Homebrew package cache)
.cache/*
!.cache/.gitkeep

# macOS system files
.DS_Store
._*

# Editor files
*.swp
*.swo
*~

# Backup files created by install.sh
*.backup.*
